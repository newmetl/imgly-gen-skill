{
  "name": "cesdk-social-skill",
  "version": "0.2.0",
  "description": "CLI + Claude skill for AI-driven social media image generation with CE.SDK",
  "type": "module",
  "main": "dist/cli/index.js",
  "bin": {
    "cesdk-social": "dist/cli/index.js"
  },
  "scripts": {
    "install:all": "npm install && npm --prefix editor-app install",
    "build": "tsc && npm --prefix editor-app run build && node scripts/verify-build.mjs",
    "build:server": "tsc",
    "build:editor": "npm --prefix editor-app run build",
    "verify": "node scripts/verify-build.mjs",
    "dev:editor": "npm --prefix editor-app run dev",
    "dev": "tsx src/cli/index.ts",
    "start": "node dist/cli/index.js",
    "smoketest:bootstrap": "tsx scripts/smoketest-bootstrap.ts",
    "smoketest:render": "tsx scripts/smoketest-render.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cesdk/node": "^1.73.1",
    "dotenv": "^16.4.5",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
